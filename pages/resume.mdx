import Article from '@/components/Article/Article'

export const meta = {
    title: 'Резюме',
    author: 'Aleksandr Grigorii',
    readTime: 10,
    date: new Date('2020-12-02')
}

export default ({children}) => <Article meta={meta}>{children}</Article>

Привет👋, меня зовут Александр, я web-разработчик.

### Frontend

##### JavaScript

Основной стек технологий основан на использовании стандарта **ES2015+** и экосистемы библиотеки **ReactJS**⚛️.

Компоненты пишу в функциональном стиле с использованием хуков. Стараюсь писать чистый код, использую **ESLint** и **Prettier**.
Использую встроенные возможности **React** для оптимизации: memo, useMemo, useCallback.
Для переиспользования кода использую такие паттерны, как кастомные хуки, ренедер-пропсы и HOC.

Для маршрутизации использую **React Router**.

Из стейт-менеджеров на данный момент использовал только **Redux** и его экосистему (**Redux Toolkit**, **Thunk**). Изучал **Redux Saga**, в своих проектах пока не использовал. В планах изучить еще одну библиотеку для управлением состояния - **Effector**, а также подход c кэшированием библиотеки **Apollo**.

Для создания больших и маленьких форм использую встроенные возможности **ReactJS**. ([пример](https://github.com/domosedov/reactjs-custom-form/tree/master)).
Так же использовал **Formik** с валидацией **Yup**.

Делал таблицы на "чистом" React с возможностью сортировки по столбцам.

Создавал несложные анимации используя библиотеку **React Spring**. В основном это были dropdown-меню, слайдеры.

Для работы с HTTP-запросами чаще всего использую библиотеку **Axios**, реже нативный **fetch API**. Для кэширования запросов использовал **SWR** (делал с ее помощью пагинацию). 

Для иммутабельности данных использую **ImmerJS**. (В основном в связке с хуками useState/useReducer и Context API).

Тестирование компонентов: в процессе изучения библиотека **React Testing Library**.

Для Server Side Rendering и Static Site Generation использую **NextJS**.

На данный момент мне больше всего нравится разарботка React-приложений именно с использванием **NextJS**.
Так как он предлагает удобную систему маршрутизации, разделение кода, гибридную SSG и SSR, встроенную поддержку **TypeScript, PostCSS, Sass**, SEO и многое другое.
Возможно в будущем, если моих знаний и навыков **NodeJS** будет хватать для реализации эффективного SSR, то от **NextJS** смогу отказаться.

Есть понимание основ работы систем бандлинга и транспилятора **Babel** (*понимаю зачем и для чего они нужны, но в то, как они работают "под капотом" пока не углублялся*😁). 
Cам обычно конфигурационные файлы не пишу, доверяя это дело таким CLI-инструментам как CRA, next-create-app и т.п.
Если мне требуется система соборки для какого-то sandbox-проекта, то обычно использую **Parcel**📦, либо пишу несложные конфиги для **Webpack**. 

##### TypeScript

В данный момент сфокусирован на изучении **TypeScript**. Постепенно переписывая на него некоторые свои проекты. Будущие проекты также планирую писать сразу на нем.

##### HTML и CSS💅

Есть опыт адптивной верстки с использванием Flexbox, Grid (Mobile-First подход), а так же поддержка кросс-браузерности.

В последнее время стал больше внимания уделять доступности контента. Планирую глубже изучить специфичтность html-тэгов, а также изучить ARIA-атрибуты.

В своих проектах использовал **Sass**(оба синтаксиса): модульность, переменные, вложенность селекторов, миксины и т.д.

Использовал для CSS **БЭМ**-методологию именования классов.

Был небольшой опыт работы с **Bootstrap 4**.

В **React** использовал подход **CSS/SASS-модулей**. Так же знаком с **CSS-in-JS** (**React Styled Components**). 

В данный момент для стилизации компонентов активно использую **PostCSS** и **TailwindCSS**. С помощью **TailwindCSS** легко реализую адаптивность, поддержку темных тем. 

### Backend

##### NodeJS

Моя цель - стать Fullstack JavaScript-разработчком. 

Сейчас я на активной стадии изучени **NodeJS**, а также СУБД **PostgreSQL**.

В своих sandbox-проектах на NodeJS использовал ORM **Prisma**. В планах познакомится/изучить **TypeORM**.

Недавно изучал фреймворк **ExpressJS**. Сейчас моих знаний хватает для создания несложного  **CRUD REST API** сервиса с использованием **JWT-авторизации**.

Очень интересна технология **GraphQL**, есть базовые знания, но планирую глубоко ее изучить и научиться создавать масштабируемые GraphQL-сервера (возможно на основе **Apollo**).

В планах изучить фреймворк **NestJS**, так как сейчас много времени уделяю изучению архитектуры web-приложений, ООП. А этот фреймворк (из тех, который мне известен на данный момент) даёт "правильную" базовую структуру для построения крупных приложений.

##### PHP и WordPress

Основы **PHP7, ООП, паттерны проектирования**.

Есть опыт работы в создании темы для **WordPress**, а так же опыт создания плагинов. Хорошо знаком с концепцией хуков, умение пользоваться документацией.

Умею эффективно работать с WP_Query.

Без использования сторонних плагинов могу сделать кастомные модели данных (Custom Post Types), роли пользователей, дополнительные мета-поля, таксономии, конечные точки REST API с пагинацией и фильтрами, кастомные SQL-запросы к БД и многое другое.

Также для разрабоки WordPress-приложений использую **Sage Starter Theme, Bedrock, Composer, LocalWP**.

В свободное время делаю свой плагин для регистрации и авторизации с использванием WordPress REST API.

##### Рабочее окружение💻

Базовое использование **Git** и [**GitHub**](https://github.com/domosedov?tab=repositories).

В качестве операционной системы использую **Windows 10 Pro** и **Ubuntu 20.04**. Работаю в Ubuntu, активно использую терминал с оболочкой **ZSH**.
Код пишу в **VS Code, WebStorm, PhpStorm**.


##### Прочие навыки

Работа с Markdown и MDX.

Выгрузка сайта на хостинг, настройка домена, Let's Encrypt сертификата, подключение SMTP, базовая развертка NodeJS/PHP-приложения на VPS/VDS.
